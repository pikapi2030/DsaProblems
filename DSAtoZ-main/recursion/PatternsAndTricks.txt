1)
if we need to count number of sunsequences with sum=k

if(base case)
{
    return 1;
}
else (return 0;)

int left=F(left call)
int right=F(right call)

return left+right;

2) store all subsequence with sum k

if(index==n)//here index does not represent size of temp but where are we at our main array in terms of pick and not-pick
{
if(sum==k)
{
    store the subsequence
}

}

temp.push(index)//pick
sum+=v[index]//add the sum
F(index+1,temp)//call
temp.pop_back(index)//not pick (need to pop as temp is passed by reference, so to not pick I have to remove the last picked )
sum-=v[index]//remove the sum
F(index+1,temp)//call the function

3) to store only one subsequence with sum k (any possible subsequence)

we will make a boolean function
if(index=n)
{
if(sum==k)
{
    return true;
}
else return false;
}

temp.push(v[index]);
sum+=v[index];
if(F(temp,index+1)==true)//if one subsequence is found then no need to do further operations
{
    return true;
}
temp.pop();
sum-=v[index];
if(F(temp,index+1)==true)
{
    return true;
}
return false;

Pick not pick aproach is better in calculating 
 total sum of ( anyMathsCalculation(subset_1)+anyMathsCalculation(subset_2)....)
 it can do it in O(1) SPACE(ignoring recursion stack space), without even generating the subsets
 T.C O(2^n) instead of O(n*2^n) (extra n to perform that mathmatical operation on every subset)

NOTE:Do no directly apply DP in this aproach, apply DP where return 0; in base case(explained below)
HERE * REFERS AS ANY BINARY OPERATOR (+,-,xor ..)

if(index==n){return ans;}
int pick=helepr(nums,ans*nums[index],index+1)
int notPick=helepr(nums,ans,index+1)

retrun pick+not pick

EXAMPLE SEE: sumOfALLsubsetXOR (in bit manuplation FOLDER)

BETTER way to use pick, not pick to accumulate ans( also dp friendly)
SEE House Robber problem

int helper(vector<int> &v, int cur)
{
    if (cur >= v.size())
    {
        return 0;
    }
    int pick = v[cur] + helper(v, cur + 1);

    int notPick = helper(v, cur + 1);
    return max(pick, notPick);
}

int bruteR(vector<int> &v)
{
    int n = v.size();
    vector<int> temp;
    return helper(v, 0);
}

